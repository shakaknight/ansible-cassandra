---
# tasks file for test
- name: Print the hostname
  debug:
    msg: "{{var}}"
  # debug:
  #   msg: "Hostname: {{host_name}}"
  #   # {{group_names}}
  # when: '"elasticMaster" in group_names|string'